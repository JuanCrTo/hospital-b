import { Injectable } from '@nestjs/common';

@Injectable()
export class PdfService {
    constructor() {}

    // generateMedicalReport (patientId: string, doctorId: string)
    // generateLabResult (patientId: string, nurseId: string)
    // generateCertificate (patientId: string, nurseId: string)
    // generateFullHistory (patientId: string)
}
